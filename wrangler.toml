name = "chess-com-rating"
main = "build/index.js"
compatibility_date = "2026-06-14"

[build]
command = "cargo install -q \"worker-build@^0.8\" && worker-build --release"

[observability.logs]
enabled = true

[[tail_consumers]]
service = "axiom-tail-worker"
